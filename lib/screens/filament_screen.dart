import 'package:flutter/material.dart';

class FilamentScreen extends StatelessWidget {
  const FilamentScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cómo usar el filamento')),
      body: const Center(child: Text('Contenido de Cómo usar el filamento')),
    );
  }
}
